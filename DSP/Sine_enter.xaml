<Window x:Class="DSP.Sine_enter"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DSP"
        mc:Ignorable="d"
        Title="Sine_enter" Height="200" Width="350">
    <Grid>
        <Grid.RowDefinitions>

        </Grid.RowDefinitions>
        <Grid>
            <TextBox Name="amplitude" Text="Амплитуда"></TextBox>
            <TextBox Name="phase" Text="Начальная фаза" Margin="0,44,0,85"></TextBox>
            <TextBox Name="round_freq" Text="Круговая частота" Margin="0,92,0,33"></TextBox>
        </Grid>
        <Grid>
            <Button Content="Смоделировать сигнал" Margin="0,150,0,0" Click="sine_modultaion"/>
        </Grid>
    </Grid>
</Window>
